import styled, { css } from "styled-components";

export const StyledLottieAstronaut = styled.div`
  ${({}) => css`
    pointer-events: none;
    position: relative;
    position: relative;
    bottom: 30px;
  `}
`;
